import 'package:flutter/material.dart';

class Translator extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Memic'),
      ),
      body: Center(
        child: Text("sound like you, anywhere"),
      ),
    );
  }
}
